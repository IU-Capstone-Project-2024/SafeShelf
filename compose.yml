services:
  database:
    container_name: db
    image: postgres:16
    ports:
      - "5433:5432"
    environment:
      POSTGRES_DB: safe
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgresql:/var/lib/postgresql/data

volumes:
  postgresql: { }

networks:
  backend: { }